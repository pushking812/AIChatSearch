[pytest]
minversion = 7.0

addopts =
    -ra
    --strict-markers
    --tb=short
    --cov=deepseek
    --cov-report=term-missing
    --cov-fail-under=80

testpaths =
    tests

python_files =
    test_*.py

python_classes =
    Test*

python_functions =
    test_*