[project]
name = "aichat_search"
version = "0.1.0"
description = "AI Chat Archive Search"
requires-python = ">=3.9"

[project.scripts]
aichat_search = "aichat_search.gui:run_gui"

[tool.setuptools.packages.find]
where = ["."]