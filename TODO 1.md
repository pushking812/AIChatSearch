# TODO: Реализация поддержки нескольких источников данных

## Итерация 1 "Введение класса DataSource и рефакторинг ChatController" 
Модули: `model.py`, `controller.py`
- [ ] **Подзадача 1.1** "Создать класс `DataSource` в `model.py` (или отдельном модуле `datasource.py`)"  
  - Определить поля: `file_path: str`, `chats: List[Chat]`, `id: str` (упрощённо – basename или UUID).
- [ ] **Подзадача 1.2** "Добавить в `ChatController` атрибут `self.sources: List[DataSource]` вместо `self.chats`"
- [ ] **Подзадача 1.3** "Добавить глобальное множество `self.known_chat_ids = set()` для контроля дубликатов"
- [ ] **Подзадача 1.4** "Реализовать метод `add_source(file_path: str) -> List[Chat]`":
  - Загрузить архив через `model.load_from_zip`.
  - Для каждого нового чата проверить `id` по `known_chat_ids`.
  - Добавить только уникальные чаты в новый `DataSource` и в общее множество.
  - Добавить `DataSource` в `self.sources`.
  - Вызвать `_rebuild_filtered_chats()`.
- [ ] **Подзадача 1.5** "Реализовать метод `_rebuild_filtered_chats()` для построения объединённого отфильтрованного списка"
- [ ] **Подзадача 1.6** "Обновить метод `filter_chats()` для работы с новым механизмом (сохранять фильтр, вызывать `_rebuild_filtered_chats`)"
- [ ] **Подзадача 1.7** "Обновить методы `get_filtered_chats()`, `search()`, `search_with_positions()` – они должны работать с `self.filtered_chats` (не меняется)"
- [ ] **Подзадача 1.8** "Модифицировать `set_chats()` – теперь он должен заменять все источники (для обратной совместимости) или быть удалён"

---

## Условные обозначения
- [ ] – задача запланирована
- [V] – выполнено
- [~] – выполнено частично
- [-] – отложено
- [Х] – не будет реализовано