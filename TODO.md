# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 8: Централизация констант и путей
**Задействованные модули:** `constants.py`, `application.py`, `window_state.py`, `controller.py`
**Цель:** Единое место для всех настроек.
- [ ] В `constants.py` добавить:
  ```python
  CONFIG_DIR = ".config"
  CONFIG_FILE = "config.json"
  SESSION_FILE = "session.pkl"   # пока pickle
```
- [ ] В application.py и window_state.py использовать эти константы для построения путей.
- [ ] В ChatController использовать constants.SESSION_FILE вместо PKL_FILE.
- [ ] Убедиться, что все пути вычисляются относительно корня проекта (например, os.path.join(os.path.dirname(__file__), '..', CONFIG_DIR)).
**Риски:** Низкие, если правильно скорректировать пути. 
**После итерации:** Проверить сохранение и загрузку сессии, геометрии.