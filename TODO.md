# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 1: Устранение глобальной переменной и мелкие чистки
**Задействованные модули:** `model.py`
**Цель:** Удалить неиспользуемый глобальный объект, снизить риск побочных эффектов.
- [ ] В `model.py` удалить строку `raw_data = None`.
- [ ] В функции `load_from_zip` заменить `global raw_data` на локальную переменную (например, `data = json.load(f)`) и использовать её.
- [ ] Проверить, что нигде в проекте не используется `model.raw_data` (поиск по коду). Если используется – адаптировать.
- [ ] Удалить неиспользуемые импорты (например, `import os` в `model.py` – он нужен только для `DataSource`, оставить).
**Риски:** Низкие. Изменение только внутри модуля `model`, функционал не меняется.
**После итерации:** Проверить загрузку архива.