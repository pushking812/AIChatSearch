# TODO: Разработка приложения для поиска по архиву чатов DeepSeek (скорректированная версия)

## Условные обозначения
- [ ] – задача не начата
- [V] – выполнено
- [X] – решено не реализовывать
- [-] – отложено на потом

---

### Итерация 0: Настройка проекта
- [V] Создать структуру папок: `src/`, `tests/`. В `src/` создать пустые файлы `__init__.py`, `model.py`, `gui.py`, `utils.py`, `main.py`. В `tests/` создать `__init__.py`, `test_model.py`.
- [V] В `model.py` определить заглушки классов `Chat` и `MessagePair` (с pass).
- [V] В `gui.py` создать функцию `run_gui()` и класс `Application` (заглушка).
- [V] В `main.py` реализовать точку входа: вызов `run_gui()` при `if __name__ == "__main__"`.
- [V] В `gui.py` в классе `Application` инициализировать окно Tkinter (заголовок "DeepSeek Chat Archive Navigator", размер 1200x800), запустить `mainloop()`.

### Итерация 1: Модель данных и загрузка из ZIP
- [V] В `model.py` реализовать класс `MessagePair`:
  - Атрибуты: `index` (str, исходный id узла, например "1"), `request_text` (str), `response_text` (str), `request_time` (datetime), `response_time` (datetime), `request_node_id` (str), `response_node_id` (str), `modified` (bool, по умолчанию False).
  - Метод `__repr__` для отладки.
- [V] В `model.py` реализовать класс `Chat`:
  - Атрибуты: `id` (str), `title` (str), `created_at` (datetime), `updated_at` (datetime), `pairs` (list of MessagePair).
  - Метод `add_pair(pair)` для добавления пары в список.
  - Метод `get_pairs()` возвращает список.
- [V] В `utils.py` написать функцию `parse_datetime(iso_str: str) -> datetime` для преобразования ISO-дат (с часовым поясом) в наивный datetime (отбросить зону, т.к. она не нужна для отображения).
- [V] В `model.py` создать функцию `load_from_zip(zip_path: str) -> list[Chat]`:
  - Открыть zip-файл, прочитать `conversation.json`, распарсить.
  - Сохранить `raw_data` для последующего сохранения (глобальный объект JSON).
  - Для каждого элемента корневого массива:
    - Извлечь поля: `id`, `title`, `inserted_at`, `updated_at`.
    - Собрать все узлы из `mapping`, у которых есть `message`. Отсортировать по `inserted_at` (использовать `parse_datetime`).
    - Пройти по отсортированным узлам и сгруппировать в пары: предполагается строгое чередование REQUEST → RESPONSE. Для каждой пары создать объект `MessagePair`, сохранив тексты, времена и id узлов.
    - Создать объект `Chat` и добавить в него пары.
  - Вернуть список чатов.
- [V] Добавить базовую обработку ошибок: если пара неполная или типы не совпадают, пропустить и вывести предупреждение в консоль.
- [V] Написать тест в `tests/test_model.py`: создать небольшой JSON-файл (в виде строки), проверить, что `load_from_zip` (с имитацией zip) правильно парсит чаты и пары.
- [V] **Тестирование**: добавить тестирование загрузки архивов и корректности работы классов `Chat` и `MessagePair`.
- [V] Coverage ≥ 85%

### Итерация 2: Базовое GUI – отображение чатов и пар
- [ ] В `gui.py` доработать класс `Application`:
  - Создать главное меню: `menubar = tk.Menu(self)`, добавить пункт "Файл" с командой "Открыть архив...".
  - Разделить окно на две горизонтальные панели через `tk.PanedWindow` (ориентация горизонтальная).
- [ ] Левая панель:
  - `tk.Frame` с `tk.Listbox` и `tk.Scrollbar`. Упаковать.
  - Заголовок "Чаты".
- [ ] Правая панель: вертикальный `tk.PanedWindow` (ориентация вертикальная) с двумя фреймами.
  - Верхний фрейм: `tk.Listbox` + скролл, заголовок "Сообщения".
  - Нижний фрейм: два текстовых поля `tk.Text` с прокруткой, размещённые вертикально (можно через `tk.Frame` с `tk.Label` для каждого).
- [ ] Реализовать метод `open_archive`:
  - Вызвать `filedialog.askopenfilename(filetypes=[("ZIP files", "*.zip")])`.
  - Если файл выбран, вызвать `model.load_from_zip`, результат сохранить в `self.chats`. Также сохранить `self.archive_path` и `self.raw_data` (из функции загрузки).
  - Обновить левый список: для каждого чата добавить `chat.title` в Listbox.
- [ ] Реализовать обработчик выбора чата (`on_chat_select`):
  - Получить индекс выбранного чата, взять соответствующий `Chat` из `self.chats`.
  - Очистить правый верхний список и заполнить его строками для каждой пары: `f"#{pair.index}: {pair.request_text[:50]}... → {pair.response_text[:50]}"`.
  - Сохранить ссылку на текущий чат в `self.current_chat` и список его пар в `self.current_pairs`.
- [ ] Реализовать обработчик выбора пары (`on_pair_select`):
  - Получить индекс выбранной пары, взять `MessagePair` из `self.current_pairs`.
  - Очистить нижние текстовые поля и вставить `pair.request_text` и `pair.response_text`.
  - Сохранить `self.current_pair` и его индекс.
- [ ] Привязать события `<<ListboxSelect>>` к соответствующим спискам.

### Итерация 3: Навигация по парам внутри чата
- [ ] Под нижними текстовыми полями добавить `tk.Frame` с кнопками:
  - `tk.Button` "← Предыдущая" (команда `self.prev_pair`).
  - `tk.Button` "Следующая →" (команда `self.next_pair`).
- [ ] Реализовать `prev_pair`:
  - Если `self.current_chat` и `self.current_pair_index > 0`: уменьшить индекс, обновить выделение в списке пар (снять текущее, установить новое), вызвать отображение пары, обновить состояние кнопок.
- [ ] Реализовать `next_pair` аналогично.
- [ ] Создать метод `update_nav_buttons`:
  - Если `self.current_pair_index == 0`, кнопка "Предыдущая" неактивна, иначе активна.
  - Если `self.current_pair_index == len(self.current_pairs)-1`, кнопка "Следующая" неактивна.
- [ ] Вызывать `update_nav_buttons` при смене чата, выборе пары, и после навигации.
- [ ] **Подсветка текста при навигации**: при переходе по парам, подсвечивать искомые фрагменты в тексте, если поиск был активен.

### Итерация 4: Поиск внутри одного чата
- [ ] Над правым верхним списком добавить фрейм с элементами:
  - `tk.Entry` (переменная `self.search_var`).
  - `tk.Combobox` со значениями `["Название чата", "Запрос", "Ответ"]` (переменная `self.search_field_var`).
  - `tk.Button` "Найти" (команда `self.search_current_chat`).
  - `tk.Button` "Сбросить" (команда `self.reset_search`).
- [ ] Реализовать `search_current_chat`:
  - Получить текст поиска и выбранное поле.
  - Если поле "Запрос": отфильтровать `self.current_chat.pairs`, проверяя вхождение подстроки (регистронезависимо) в `pair.request_text`.
  - Если поле "Ответ": аналогично по `pair.response_text`.
  - Если поле "Название чата": проверить вхождение в `self.current_chat.title`. Если совпадает, оставить **все пары** текущего чата; иначе очистить список (ничего не найдено).
  - Сохранить отфильтрованный список в `self.current_pairs` и обновить правый верхний список.
  - Если ни одной пары не найдено, показать сообщение в статус-баре (пока не реализовано) или очистить список.
- [ ] Реализовать `reset_search`:
  - Восстановить `self.current_pairs = self.current_chat.pairs`, обновить список.
  - Очистить поле поиска.
- [ ] При смене чата автоматически сбрасывать поиск (вызывать `reset_search`).

### Итерация 5: Множественный выбор чатов и фильтрация списка чатов
- [ ] Над левым списком добавить `tk.Entry` (переменная `self.chat_filter_var`) с привязкой события `<KeyRelease>` к `self.filter_chats`.
- [ ] Реализовать `filter_chats`:
  - Получить фильтр из `self.chat_filter_var`.
  - Обновить левый список: оставить только те чаты из `self.chats`, у которых `title` содержит фильтр (регистронезависимо). Сохранить отфильтрованный список в `self.filtered_chats`.
- [ ] Изменить режим выбора левого списка на `tk.LISTBOX_EXTENDED`.
- [ ] Добавить под левым списком кнопки "Выбрать все" и "Снять выделение":
  - `select_all_chats`: выделить все элементы в левом списке (использовать `selection_set(0, tk.END)`).
  - `deselect_all_chats`: снять выделение (`selection_clear(0, tk.END)`).
- [ ] Модифицировать `on_chat_select`: теперь событие вызывается при любом изменении выделения. Нужно собрать все выбранные чаты (по индексам из левого списка, затем получить соответствующие объекты из `self.filtered_chats`). Сохранить список выбранных чатов в `self.selected_chats`.
- [ ] Модифицировать поиск: при нажатии "Найти" поиск идёт по всем `self.selected_chats` (если список пуст, то по всем `self.filtered_chats`). В зависимости от выбранного поля:
  - "Название чата": для каждого чата проверять вхождение в `title`; если совпадает, добавлять **все пары** этого чата в результаты.
  - "Запрос"/"Ответ": проверять соответствующие поля каждой пары и добавлять только совпавшие пары.
  - Результаты собрать в список кортежей `(chat_title, pair)` для отображения.
- [ ] Пока правый верхний список остаётся `Listbox`, отображать строки с префиксом названия чата: `f"{chat_title} [#{pair.index}]: {pair.request_text[:30]}... → {pair.response_text[:30]}"`.

### Итерация 6: Отображение результатов поиска в Treeview (группировка по чатам)
- [ ] Импортировать `ttk` из `tkinter`.
- [ ] Заменить правый верхний `Listbox` на `ttk.Treeview` с колонками: `"#"`, `"Дата ответа"`, `"Запрос"`, `"Ответ"`.
- [ ] Настроить дерево: `tree["columns"] = ("#", "date", "request", "response")`, задать заголовки и ширину.
- [ ] Реализовать функцию `display_search_results(results)`:
  - Очистить дерево (удалить все дочерние элементы).
  - Сгруппировать результаты по чатам (использовать словарь `{chat_title: list_of_pairs}`).
  - Для каждого чата создать родительский узел с текстом:  
    `f"{chat.title} (создан: {chat.created_at.strftime('%Y-%m-%d %H:%M')}, обновлён: {chat.updated_at.strftime('%Y-%m-%d %H:%M')})"`.
  - Для каждой пары в чате создать дочерний узел со значениями:
    - `pair.index` (исходный id узла),
    - `pair.response_time.strftime('%Y-%m-%d %H:%M')`,
    - превью запроса (подробнее см. ниже),
    - превью ответа.
  - Сохранить соответствие между идентификатором узла в дереве и парой (например, прикрепить объект пары к узлу через `tree.set(iid, "pair", pair)` или хранить отдельный словарь).
- [ ] Реализовать формирование превью с учётом контекста (расширенный функционал):
  - Если поиск вёлся по полю "Запрос", то для колонки "Запрос" формировать превью с контекстом вокруг искомого слова (например, до 50 символов слева и справа, выделяя троеточиями).
  - Если поиск по "Ответ" – аналогично для колонки "Ответ".
  - Если поиск по "Название чата" или поле не совпадает – показывать первые 50 символов.
  - Для этого написать вспомогательную функцию `get_context(text, search_term, context_len=50)`.
- [ ] При выборе элемента в дереве (событие `<<TreeviewSelect>>`):
  - Определить, выбран ли дочерний узел (проверить наличие родителя).
  - Получить объект `MessagePair` и отобразить его тексты в нижних полях.
  - Также сохранить ссылку на текущий чат (родительский узел) и индекс пары для навигации.

### Итерация 7: Автоматический поиск и опции
- [ ] Добавить флажок `ttk.Checkbutton` "Искать по мере ввода" рядом с полем поиска (переменная `self.auto_search_var`).
- [ ] Привязать к полю поиска событие `<KeyRelease>`: если флажок установлен, запускать таймер для выполнения поиска через 300 мс.
- [ ] Реализовать механизм отмены предыдущего таймера: хранить `self.search_after_id`, при новом вводе отменять `after_cancel`, запускать новый.
- [ ] Если флажок снят, поиск только по кнопке "Найти".
- [ ] При выполнении поиска (вручную или авто) использовать ту же логику, что и в итерации 5/6, но обновлять дерево результатов.

### Итерация 8: Подсветка искомого текста
- [ ] В нижних текстовых полях настроить тег "search" с фоном yellow (`text_widget.tag_configure("search", background="yellow")`).
- [ ] При отображении пары (в `on_pair_select` или при выборе в дереве) получить текущий поисковый запрос и выбранное поле.
- [ ] Если поле поиска не "Название чата" и поисковый запрос не пуст:
  - Для поля запроса найти все вхождения подстроки (регистронезависимо) с помощью `text.search` и применить тег.
  - Для поля ответа аналогично (если поиск по ответу, подсвечивать в ответе; если по запросу — в запросе; можно подсвечивать в обоих полях для универсальности, но концепция предполагает подсветку только в соответствующем поле, поэтому будем делать так: если искали по запросу — подсвечиваем в запросе, если по ответу — в ответе, если по названию — не подсвечиваем).
- [ ] Очищать теги перед новым выделением: `text_widget.tag_remove("search", "1.0", tk.END)`.
- [ ] Если поле поиска пусто или выбрано "Название чата", ничего не подсвечивать.

### Итерация 9: Редактирование текста и сохранение в памяти
- [ ] Убедиться, что текстовые поля доступны для редактирования (по умолчанию `state=NORMAL`).
- [ ] Добавить кнопку "Сохранить изменения" под текстовыми полями.
- [ ] Реализовать `save_current_pair`:
  - Получить текущий объект `MessagePair`.
  - Извлечь тексты из полей и сравнить с исходными. Если отличаются, обновить `pair.request_text` и/или `pair.response_text`.
  - Установить флаг `pair.modified = True`.
  - Обновить отображение в дереве: найти соответствующий узел и изменить текст в колонках (превью). Добавить символ "*" к индексу пары в дереве, чтобы показать изменение.
- [ ] Добавить в `MessagePair` атрибут `modified` (по умолчанию False) и при изменении устанавливать True.
- [ ] При сохранении архива позже будем использовать флаг для определения, что нужно обновить в JSON.

### Итерация 10: Сохранение в архив (расширенная функциональность)
- [ ] В классе `Application` хранить `self.archive_path` и `self.raw_data` (исходный распарсенный JSON).
- [ ] При загрузке архива в итерации 1 дополнительно сохранять `self.raw_data` (глобальный объект JSON). Также при создании `MessagePair` сохранять `node_id` для запроса и ответа (уже сделано).
- [ ] В меню "Файл" добавить пункты "Сохранить" и "Сохранить как...".
- [ ] Реализовать метод `save_to_zip(self, save_path=None)`:
  - Если `save_path` не указан, использовать `self.archive_path`.
  - Создать временную папку (`tempfile.mkdtemp`).
  - Извлечь все файлы из оригинального архива во временную папку.
  - Обновить `conversation