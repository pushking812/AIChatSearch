# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 7: Улучшение представления списка чатов (Treeview вместо Listbox)
**Задействованные модули:** `chat_list.py`
**Цель:** Повысить информативность списка чатов, группировка по источникам.
- [ ] В `chat_list.py` заменить `Listbox` на `ttk.Treeview` с колонками "Источник", "Название чата".
- [ ] Метод `update_list(items)` теперь будет заполнять дерево, группируя по источнику. Для этого можно создать словарь `source_name -> list of chats`.
- [ ] Метод `get_selected_chats()` должен собирать выбранные элементы (чаты) из дерева (учитывая, что могут быть выбраны как чаты, так и группы? Пока поддерживаем только выбор чатов, группы не выбираем).
- [ ] Обновить методы `select_all` и `clear_selection` для работы с деревом.
- [ ] В `application.py` вызов `self.chat_panel.update_list(items)` остаётся без изменений, так как интерфейс не меняется.
**Риски:** Средние. Изменяется внутренняя реализация панели, но публичный API (`update_list`, `get_selected_chats`) остаётся тем же. Нужно тщательно протестировать множественный выбор, фильтрацию и снятие выделения.
**После итерации:** Убедиться, что список чатов отображается с группировкой, выбор работает как раньше.