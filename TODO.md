# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 3: Выделение сервиса поиска
**Задействованные модули:** `controller.py`, `services/search_service.py` (новый)
**Цель:** Отделить логику поиска от контроллера.
- [ ] Создать `services/search_service.py` с классом `SearchService`.
- [ ] Перенести методы `search` и `search_with_positions` из `ChatController` в `SearchService`. Методы могут принимать необходимые параметры (чат, запрос, поле).
- [ ] В `ChatController` добавить атрибут `self._search_service = SearchService()`.
- [ ] Заменить вызовы `self.search(...)` и `self.search_with_positions(...)` внутри контроллера на `self._search_service.search(...)` и т.д. (контроллер теперь делегирует).
- [ ] В `gui_components/search_controller.py` оставить как есть – он продолжает вызывать `controller.search_with_positions`.
**Риски:** Низкие, если правильно передать параметры. Методы контроллера становятся обёртками.
**После итерации:** Проверить поиск по запросу и ответу, навигацию по результатам.
