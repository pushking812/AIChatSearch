# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 2: Выделение сервиса загрузки архивов
**Задействованные модули:** `model.py`, `controller.py`, `services/archive_loader.py` (новый), `services/__init__.py` (новый)
**Цель:** Вынести логику парсинга ZIP из модели в отдельный сервис.
- [ ] Создать пакет `deepseek/services/` (если нет) и файл `archive_loader.py`.
- [ ] Перенести функцию `load_from_zip` из `model.py` в `archive_loader.py` (можно оставить как функцию, но лучше класс `ArchiveLoader` со статическим методом).
- [ ] В `model.py` удалить функцию, оставить только классы данных.
- [ ] В `controller.py` изменить импорт: `from ..services.archive_loader import load_from_zip`.
- [ ] Обновить вызов в `add_source` (он использует `load_from_zip`).
**Риски:** Низкие, чистое перемещение кода. Проверить импорты, не возникло ли циклических зависимостей.
**После итерации:** Загрузить архив, убедиться, что чаты появляются.