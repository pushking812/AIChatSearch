# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 9: (Опционально) Подготовка к замене pickle на JSON
**Задействованные модули:** `services/session_manager.py`
**Цель:** Заложить основу для будущей сериализации в JSON, но пока оставить pickle.
- [ ] В SessionManager создать отдельные методы _to_dict и _from_dict для преобразования объектов DataSource, Chat, MessagePair в словари и обратно.
- [ ] В методах save и load использовать эти преобразования, но пока оборачивать в pickle (т.е. сериализовать словари в pickle или оставить как есть). Это позволит в будущем легко переключиться на json.dump.
- [ ] Добавить проверку версии формата.
**Риски:** Средние, требует аккуратного написания методов преобразования. Не нарушает текущую работу.
**После итерации:** Эту итерацию можно отложить до момента, когда будет принято окончательное решение отказаться от pickle.