# TODO: Рефакторинг DeepSeek Navigator

## Общие принципы
- Каждая итерация сохраняет полную работоспособность приложения.
- После каждой итерации проводится ручное тестирование основных сценариев:
  - загрузка архива,
  - фильтрация чатов,
  - выбор чата/сообщения,
  - поиск и навигация по результатам,
  - редактирование и сохранение,
  - сохранение/загрузка сессии,
  - восстановление геометрии окна.

---

## Итерация 6: Рефакторинг ChatController – разбивка методов и улучшение читаемости
**Задействованные модули:** `controller.py`
**Цель:** Улучшить внутреннюю структуру контроллера без изменения публичного API.
- [ ] Разбить метод `add_source` на более мелкие: `_load_new_chats(file_path)`, `_filter_unique(chats)`, `_add_source_with_chats(chats)`.
- [ ] Разбить `_rebuild_filtered_chats` на `_collect_all_chats()` и `_apply_filter()`.
- [ ] Переименовать некоторые внутренние переменные для ясности (например, `_chat_to_source` → `_chat_source_map`).
- [ ] Убрать неиспользуемый метод `set_chats` (если он нигде не вызывается). Проверить, используется ли он в `gui.py` или других местах. Если используется, оставить как есть.
- [ ] Добавить аннотации типов для методов.
**Риски:** Низкие, если изменения только внутренние. После рефакторинга прогнать основные сценарии.